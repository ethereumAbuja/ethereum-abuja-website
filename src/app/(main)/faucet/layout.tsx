import { ReactNode } from "react";

export const metadata = {
  title: "ETHAbuja | Donation Faucet",
};

export default function DonationLayout({ children }: { children: ReactNode }) {
  return <>{children}</>;
}
