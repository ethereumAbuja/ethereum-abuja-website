import React from "react";
import AboutUI from './aboutUI'

export const metadata = {
  title: "ETHAbuja | About",
};

export default async function About() {
  return <AboutUI pipelineData={null} />;
}
