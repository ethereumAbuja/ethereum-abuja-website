import React from "react";
import ShowcaseUI from './showcaseUI'

export const metadata = {
  title: "ETHAbuja | Talks",
};

export default async function Showcase() {
  return <ShowcaseUI pipelineData={null} />;
}
