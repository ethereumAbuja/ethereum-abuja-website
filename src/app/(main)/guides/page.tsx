import React from "react";
import GuideUI from './guideUI'

export const metadata = {
  title: "ETHAbuja | Guides",
};

export default async function Guide() {
  return <GuideUI pipelineData={null} />;
}
